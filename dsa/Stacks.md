
- `push(x)` - adds x to the top
- `pop()` - removes and returns the top
- `peek()` or `top()` - just looks at the top without removing
- `isEmpty()` - checks if the stack is empty
- `size()` - returns number of elements

